# US-004: 使用 Pod 进行服务器部署

| 属性 | 值 |
|------|-----|
| **状态** | Draft |
| **作者** | AgentsMesh Team |
| **创建日期** | 2026-01-16 |
| **角色** | DevOps 工程师 / 开发者 |

---

## 1. 概述

### 1.1 用户故事

**作为** 一名 DevOps 工程师或开发者，
**我希望** 通过 AgentsMesh Pod 执行服务器部署任务，
**以便** 让 AI Agent 自动化处理部署流程，减少人工操作，降低部署风险。

### 1.2 价值主张

- **自动化部署**: AI Agent 理解部署意图，自动执行复杂操作
- **智能故障处理**: 遇到问题时自动诊断并尝试修复
- **实时可观测**: 全程监控部署进度和服务器状态
- **安全可控**: 敏感操作需确认，支持回滚机制

---

## 2. 部署场景

### 2.1 支持的部署类型

| 类型 | 说明 | 典型工具 |
|------|------|----------|
| **应用部署** | 部署应用代码到服务器 | SSH, rsync, Docker |
| **容器部署** | 部署到 Kubernetes/Docker 环境 | kubectl, docker-compose |
| **基础设施** | 配置服务器、网络、存储 | Ansible, Terraform |
| **数据库变更** | 执行数据库迁移脚本 | migrate, flyway |
| **配置更新** | 更新配置文件、环境变量 | SSH, ConfigMap |

### 2.2 部署目标环境

```
┌─────────────────────────────────────────────────────────────────┐
│                        AgentsMesh                               │
│  ┌──────────────┐                                               │
│  │  Deploy Pod  │                                               │
│  │ (Claude Code)│                                               │
│  └──────┬───────┘                                               │
└─────────┼───────────────────────────────────────────────────────┘
          │
          │ SSH / kubectl / API
          │
    ┌─────┴─────┬─────────────┬─────────────┐
    ▼           ▼             ▼             ▼
┌────────┐ ┌────────┐   ┌──────────┐   ┌────────┐
│ Dev    │ │Staging │   │Production│   │  K8s   │
│Server  │ │Server  │   │ Server   │   │Cluster │
└────────┘ └────────┘   └──────────┘   └────────┘
```

---

## 3. 前置条件

| 条件 | 说明 |
|------|------|
| Runner 已部署 | Runner 需要有访问目标服务器的网络权限 |
| 凭证已配置 | SSH 密钥、kubeconfig、云服务凭证等已配置 |
| 部署脚本就绪 | 项目中存在部署相关脚本或配置文件 |
| 权限已授予 | Runner 运行用户有执行部署操作的权限 |

---

## 4. 用户流程

### 4.1 整体流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  选择环境   │ ──► │  配置部署   │ ──► │  启动 Pod   │ ──► │  执行部署   │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                                                                    │
                                                                    ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  部署完成   │ ◄── │  健康检查   │ ◄── │  监控进度   │ ◄── │  连接服务器 │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 4.2 详细步骤

#### Step 1: 选择部署环境

1. 进入项目部署页面
2. 选择目标环境：
   - **Development**: 开发环境
   - **Staging**: 预发布环境
   - **Production**: 生产环境（需额外确认）
3. 查看环境配置和当前状态

**环境配置示例:**
```yaml
environments:
  development:
    servers:
      - host: dev.example.com
        user: deploy
        port: 22
    deploy_path: /var/www/app
    branch: develop

  staging:
    servers:
      - host: staging.example.com
        user: deploy
        port: 22
    deploy_path: /var/www/app
    branch: main

  production:
    servers:
      - host: prod-1.example.com
        user: deploy
        port: 22
      - host: prod-2.example.com
        user: deploy
        port: 22
    deploy_path: /var/www/app
    branch: main
    requires_approval: true
```

#### Step 2: 配置部署参数

1. 选择部署版本（分支/Tag/Commit）
2. 配置部署选项：
   - 是否执行数据库迁移
   - 是否清理缓存
   - 是否重启服务
   - 滚动部署 vs 全量部署
3. 设置部署超时时间

**部署配置 UI:**
```
┌─────────────────────────────────────────────────────────────────┐
│ 部署配置                                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 目标环境:     [Staging ▼]                                       │
│ 部署版本:     [main ▼]  commit: a1b2c3d                         │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 部署选项                                                     │ │
│ │ ☑ 执行数据库迁移                                             │ │
│ │ ☑ 清理应用缓存                                               │ │
│ │ ☑ 重启服务                                                   │ │
│ │ ☐ 滚动部署 (逐台更新)                                        │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 超时时间:     [30] 分钟                                         │
│                                                                 │
│                              [取消]  [开始部署]                  │
└─────────────────────────────────────────────────────────────────┘
```

#### Step 3: 启动部署 Pod

1. 系统创建部署专用 Pod
2. 加载部署凭证和配置
3. Agent 准备部署环境

**Pod 配置:**
```yaml
pod:
  type: deployment
  agent: claude-code

  sandbox:
    type: tempdir  # 部署任务使用临时目录

  secrets:
    - name: SSH_PRIVATE_KEY
      from: vault/deploy/ssh-key
    - name: KUBECONFIG
      from: vault/deploy/kubeconfig

  env:
    DEPLOY_ENV: staging
    DEPLOY_VERSION: a1b2c3d

  capabilities:
    - ssh
    - kubectl
    - docker
```

#### Step 4: Agent 执行部署

1. Agent 分析部署任务
2. 生成部署计划并展示
3. 用户确认后执行

**Agent 部署计划:**
```
[部署计划分析]

目标: 部署 main 分支 (commit: a1b2c3d) 到 Staging 环境

执行步骤:
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: 预检查                                                  │
│ - 验证 SSH 连接                                                 │
│ - 检查磁盘空间                                                  │
│ - 验证当前服务状态                                              │
├─────────────────────────────────────────────────────────────────┤
│ Step 2: 备份                                                    │
│ - 备份当前代码版本                                              │
│ - 备份数据库 (如有迁移)                                         │
├─────────────────────────────────────────────────────────────────┤
│ Step 3: 部署代码                                                │
│ - git pull origin main                                         │
│ - 安装依赖: pnpm install --frozen-lockfile                     │
│ - 构建应用: pnpm build                                         │
├─────────────────────────────────────────────────────────────────┤
│ Step 4: 数据库迁移                                              │
│ - 执行: migrate -path ./migrations up                          │
├─────────────────────────────────────────────────────────────────┤
│ Step 5: 重启服务                                                │
│ - systemctl restart app                                        │
│ - 清理缓存: redis-cli FLUSHDB                                  │
├─────────────────────────────────────────────────────────────────┤
│ Step 6: 健康检查                                                │
│ - 检查服务端口: curl http://localhost:3000/health              │
│ - 验证关键接口响应                                              │
└─────────────────────────────────────────────────────────────────┘

预计耗时: 5-10 分钟

⚠️ 此操作将更新 Staging 服务器，是否继续？ [确认部署]
```

#### Step 5: 实时监控部署进度

1. 显示实时终端输出
2. 进度条展示各阶段状态
3. 关键操作高亮显示

**部署监控界面:**
```
┌─────────────────────────────────────────────────────────────────┐
│ 部署进度 - Staging 环境                                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ✓ Step 1: 预检查              完成 (12s)                        │
│ ✓ Step 2: 备份                完成 (45s)                        │
│ → Step 3: 部署代码            进行中...                         │
│   ├── git pull               ✓                                 │
│   ├── pnpm install           ✓                                 │
│   └── pnpm build             → 构建中 (67%)                    │
│ ○ Step 4: 数据库迁移          等待中                            │
│ ○ Step 5: 重启服务            等待中                            │
│ ○ Step 6: 健康检查            等待中                            │
│                                                                 │
│ 总进度: ████████████░░░░░░░░  55%                               │
│ 已用时: 2 分 15 秒                                               │
├─────────────────────────────────────────────────────────────────┤
│ 终端输出:                                                       │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ $ pnpm build                                                │ │
│ │ > app@1.0.0 build                                           │ │
│ │ > next build                                                │ │
│ │                                                             │ │
│ │ ▲ Next.js 14.0.0                                            │ │
│ │ Creating an optimized production build...                   │ │
│ │ ✓ Compiled successfully                                     │ │
│ │ ✓ Collecting page data                                      │ │
│ │ ✓ Generating static pages (23/45)                           │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ [查看完整日志]                                    [取消部署]     │
└─────────────────────────────────────────────────────────────────┘
```

#### Step 6: 健康检查与验证

1. Agent 执行健康检查
2. 验证服务正常运行
3. 运行冒烟测试（可选）

**健康检查输出:**
```
[健康检查报告]

服务状态检查:
✓ 应用进程运行中 (PID: 12345)
✓ 端口 3000 监听正常
✓ 数据库连接正常 (响应时间: 5ms)
✓ Redis 连接正常 (响应时间: 2ms)

接口验证:
✓ GET /health          200 OK (15ms)
✓ GET /api/v1/status   200 OK (23ms)
✓ POST /api/v1/ping    200 OK (18ms)

版本确认:
✓ 部署版本: a1b2c3d
✓ 构建时间: 2026-01-16 10:30:00

冒烟测试:
✓ 用户登录流程      通过
✓ 首页加载         通过
✓ API 基础功能      通过

═══════════════════════════════════════════════════════════════
                    ✓ 部署成功完成
═══════════════════════════════════════════════════════════════
```

---

## 5. Kubernetes 部署场景

### 5.1 K8s 部署流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 选择集群   │ ──► │ 配置部署   │ ──► │  Apply YAML │ ──► │  监控 Pods  │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                                                                    │
                                                                    ▼
                                        ┌─────────────┐     ┌─────────────┐
                                        │  部署完成   │ ◄── │ Rollout 完成│
                                        └─────────────┘     └─────────────┘
```

### 5.2 Agent K8s 操作示例

**用户指令:**
```
部署最新的 backend 镜像到 production 集群，
使用滚动更新策略，确保零停机。
```

**Agent 执行:**
```
[Kubernetes 部署执行]

1. 验证集群连接
$ kubectl cluster-info
Kubernetes control plane is running at https://k8s.example.com:6443
✓ 集群连接正常

2. 检查当前部署状态
$ kubectl get deployment backend -n production
NAME      READY   UP-TO-DATE   AVAILABLE
backend   3/3     3            3
✓ 当前有 3 个副本运行中

3. 更新镜像版本
$ kubectl set image deployment/backend \
    backend=registry.example.com/backend:v1.2.3 \
    -n production
deployment.apps/backend image updated
✓ 镜像已更新

4. 监控滚动更新
$ kubectl rollout status deployment/backend -n production
Waiting for deployment "backend" rollout to finish:
  1 out of 3 new replicas have been updated...
  2 out of 3 new replicas have been updated...
  3 out of 3 new replicas have been updated...
  Waiting for 1 old replicas to be terminated...
  All replicas updated successfully
✓ 滚动更新完成

5. 验证新版本
$ kubectl get pods -n production -l app=backend
NAME                       READY   STATUS    AGE
backend-7d8f9b6c4d-abc12   1/1     Running   45s
backend-7d8f9b6c4d-def34   1/1     Running   30s
backend-7d8f9b6c4d-ghi56   1/1     Running   15s
✓ 所有 Pods 运行正常

[部署完成] 新版本 v1.2.3 已成功部署到 production 集群
```

---

## 6. 安全机制

### 6.1 敏感操作确认

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ 敏感操作确认                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 即将执行以下操作:                                                │
│                                                                 │
│ 环境: Production (生产环境)                                      │
│ 操作: 重启服务                                                   │
│ 影响: 可能造成短暂服务中断                                       │
│                                                                 │
│ 命令预览:                                                       │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ systemctl restart app                                       │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 请输入确认码 [PROD-RESTART] 继续:                               │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                                                             │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│                              [取消]  [确认执行]                  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 权限控制

| 操作类型 | Development | Staging | Production |
|----------|-------------|---------|------------|
| 查看状态 | ✓ 允许 | ✓ 允许 | ✓ 允许 |
| 部署代码 | ✓ 允许 | ✓ 允许 | ⚠️ 需审批 |
| 数据库迁移 | ✓ 允许 | ⚠️ 需确认 | ⚠️ 需审批 |
| 重启服务 | ✓ 允许 | ⚠️ 需确认 | ⚠️ 需审批+确认码 |
| 回滚操作 | ✓ 允许 | ✓ 允许 | ✓ 允许 (紧急) |

### 6.3 凭证管理

```yaml
# 凭证配置 (存储在安全的 Vault 中)
credentials:
  ssh:
    type: ssh_key
    path: vault/deploy/ssh-key
    allowed_hosts:
      - "*.example.com"

  kubernetes:
    type: kubeconfig
    path: vault/deploy/kubeconfig
    allowed_contexts:
      - staging
      - production

  docker_registry:
    type: docker_config
    path: vault/deploy/docker-config
```

---

## 7. 回滚机制

### 7.1 自动回滚触发条件

| 条件 | 触发动作 |
|------|----------|
| 健康检查连续失败 3 次 | 自动回滚到上一版本 |
| 部署超时 | 提示用户选择回滚或继续 |
| 错误率突增 (>5%) | 告警并建议回滚 |
| 用户手动触发 | 立即执行回滚 |

### 7.2 回滚执行流程

```
[回滚执行]

触发原因: 健康检查失败 - /health 返回 503

回滚目标: v1.2.2 (上一个稳定版本)

执行步骤:
1. ✓ 停止当前部署
2. ✓ 恢复代码到 v1.2.2
3. ✓ 回滚数据库迁移 (如适用)
4. ✓ 重启服务
5. ✓ 健康检查通过

回滚完成，服务已恢复到 v1.2.2
当前状态: 正常运行

建议: 请检查 v1.2.3 的变更，修复问题后重新部署
失败日志已保存: /var/log/deploy/2026-01-16-failed.log
```

---

## 8. 系统架构

### 8.1 部署数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                           Web UI                                │
│              (部署配置 / 进度监控 / 日志查看)                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │ WebSocket
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                          Backend                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Deploy Svc  │  │ Credential  │  │ Audit Log   │             │
│  │             │  │   Vault     │  │             │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└───────────────────────────┬─────────────────────────────────────┘
                            │ WebSocket
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Runner                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                     Deploy Pod                           │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │   │
│  │  │ SSH     │  │ kubectl │  │ Docker  │  │ Ansible │    │   │
│  │  │ Client  │  │         │  │ CLI     │  │         │    │   │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘    │   │
│  └───────┼────────────┼────────────┼────────────┼──────────┘   │
└──────────┼────────────┼────────────┼────────────┼──────────────┘
           │            │            │            │
           ▼            ▼            ▼            ▼
      ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
      │ Server │   │  K8s   │   │ Docker │   │ Server │
      │  (SSH) │   │Cluster │   │Registry│   │ Group  │
      └────────┘   └────────┘   └────────┘   └────────┘
```

---

## 9. 验收标准

| # | 标准 | 验证方式 |
|---|------|----------|
| 1 | 支持 SSH 连接到目标服务器 | 连接测试 |
| 2 | 支持 kubectl 操作 K8s 集群 | 集群操作测试 |
| 3 | 部署日志实时展示，延迟 < 1 秒 | 性能测试 |
| 4 | 生产环境部署需要额外确认 | 权限测试 |
| 5 | 健康检查失败自动触发回滚 | 故障注入测试 |
| 6 | 部署历史可追溯 | 审计日志验证 |
| 7 | 凭证安全存储，不明文展示 | 安全审计 |

---

## 10. 异常处理

| 场景 | 处理方式 |
|------|----------|
| SSH 连接失败 | 检查网络和凭证，提示用户排查 |
| 磁盘空间不足 | 部署前检查，空间不足时阻止部署 |
| 构建失败 | 显示错误日志，终止部署 |
| 数据库迁移失败 | 自动回滚迁移，通知用户 |
| 服务启动失败 | 收集日志，触发回滚流程 |
| 健康检查超时 | 重试 3 次后触发回滚 |
| 网络中断 | 保存状态，支持断点续传 |

---

## 11. 部署报告

### 11.1 成功报告

```
═══════════════════════════════════════════════════════════════
                    部署报告 - 成功
═══════════════════════════════════════════════════════════════

部署信息:
- 环境: Staging
- 版本: v1.2.3 (commit: a1b2c3d)
- 操作人: user@example.com
- 开始时间: 2026-01-16 10:30:00
- 完成时间: 2026-01-16 10:35:23
- 总耗时: 5 分 23 秒

执行摘要:
┌─────────────────────────────────────────────────────────────┐
│ 阶段           │ 状态   │ 耗时    │ 备注                    │
├─────────────────────────────────────────────────────────────┤
│ 预检查         │ ✓ 通过 │ 12s    │                         │
│ 备份           │ ✓ 完成 │ 45s    │ backup-20260116-103000  │
│ 代码部署       │ ✓ 完成 │ 2m 30s │ 含依赖安装和构建         │
│ 数据库迁移     │ ✓ 完成 │ 8s     │ 执行了 2 个迁移          │
│ 服务重启       │ ✓ 完成 │ 15s    │                         │
│ 健康检查       │ ✓ 通过 │ 23s    │ 所有端点响应正常         │
└─────────────────────────────────────────────────────────────┘

变更内容:
- 新增文件: 5 个
- 修改文件: 12 个
- 数据库: +2 迁移

服务状态:
- URL: https://staging.example.com
- 响应时间: 45ms (P95)
- 错误率: 0%

═══════════════════════════════════════════════════════════════
```

### 11.2 失败报告

```
═══════════════════════════════════════════════════════════════
                    部署报告 - 失败 (已回滚)
═══════════════════════════════════════════════════════════════

部署信息:
- 环境: Staging
- 目标版本: v1.2.3
- 回滚版本: v1.2.2

失败原因:
健康检查失败 - 应用启动后 /health 端点返回 503

错误详情:
┌─────────────────────────────────────────────────────────────┐
│ Error: Connection refused to database                       │
│ at DatabasePool.connect (src/db/pool.ts:45)                │
│ at Application.init (src/app.ts:23)                        │
│                                                             │
│ 原因分析: 数据库连接字符串配置错误                           │
│ 配置项: DATABASE_URL                                        │
│ 当前值: postgres://user:***@wrong-host:5432/db             │
└─────────────────────────────────────────────────────────────┘

回滚执行:
✓ 代码已回滚到 v1.2.2
✓ 数据库迁移已回滚
✓ 服务已重启
✓ 健康检查通过

建议修复:
1. 检查 DATABASE_URL 环境变量配置
2. 确认数据库服务器地址是否正确
3. 验证网络连通性

日志文件: /var/log/deploy/2026-01-16-103000-failed.log

═══════════════════════════════════════════════════════════════
```

---

## 12. 后续迭代

- [ ] 蓝绿部署支持：零停机切换
- [ ] 金丝雀发布：渐进式流量切换
- [ ] 多区域部署：跨地域同步部署
- [ ] 部署审批流程：集成企业审批系统
- [ ] 部署窗口期：限制生产部署时间段
- [ ] 自动化测试集成：部署后自动运行测试套件
- [ ] 部署指标看板：部署频率、成功率、回滚率统计
- [ ] ChatOps 集成：通过 Slack/飞书 触发和监控部署
