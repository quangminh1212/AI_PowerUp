wire_format: openai_chat_completions
supports_cache_control: false
completion_endpoint: "http://localhost:8000/v1/completions"
chat_endpoint: "http://localhost:8000/v1/chat/completions"
supports_completion: true

api_key: any-will-work

model_default_settings_ui:
  chat:
    tokenizer: fake
  completion:
    tokenizer: fake
