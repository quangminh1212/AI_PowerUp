# Architectural Decisions Log

Append-only. Every architectural / engineering decision that you expect to
matter in 3 months lands here. Immutable once written — write a new entry
to supersede an old one (link via `links: [old-id]`).

See `SCHEMA.md` for the entry contract. Use `memory.py append` to add new
entries; direct hand-edits must still pass `memory.py validate`.

<!-- Entries are appended below. Never edit or delete past entries. -->
